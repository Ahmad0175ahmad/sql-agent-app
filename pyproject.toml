[project]
name = "sql-agent-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "pandas",
    "psycopg2-binary",
    "google-genai",
    "openai",
    "python-dotenv",
]